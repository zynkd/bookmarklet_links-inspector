{
  "name": "bookmarklet_links-inspector",
  "version": "1.0.0",
  "description": "Simple bookmarklet that inspects all links on a webpage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bookmarklet src/index.js dist/bookmarklet.js",
    "watch": "nodemon --watch src/index.js --exec 'npm run build'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bookmarklet": "^3.0.0",
    "nodemon": "^3.0.1"
  }
}
